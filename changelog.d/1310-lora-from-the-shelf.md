- Swap a LoRA from your model shelf into a workflow before you run it. The run
  panel offers your shelf's adapters and puts the one you pick into the
  workflow's LoRA loader, under the name your ComfyUI has that file under; where
  a workflow chains more than one loader, you choose which one it goes into. A
  workflow with no LoRA loader says so rather than starting a run without it.
