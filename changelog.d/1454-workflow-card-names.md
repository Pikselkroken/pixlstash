- A workflow card you have not named now reads like the workflow it is:
  "Krea 2: Text to Image + FaceDetailer". It calls the model what your model
  shelf calls it rather than what the file is spelled, and it says when the
  workflow adds a face detailer or an upscale — which is what told two
  otherwise identical cards apart only in the ⓘ panel before.
