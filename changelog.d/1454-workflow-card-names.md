- A workflow card you have not named now reads like the workflow it is:
  "Krea 2: Text to Image + FaceDetailer". It calls the model whatever your
  model shelf calls it rather than naming it after the file it sits in, and it
  says on the name row when the workflow adds a face detailer or an upscale.
  The model shelf's name is now used wherever a card shows a model, so the name
  row and the chips under it no longer call one model two different things.
