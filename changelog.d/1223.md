- Watch folders and reference folders can no longer overlap each other, in
  either direction, and neither may sit inside a library's folder or contain
  one. A library can no longer be added, from the app or the command line,
  inside or around a watch or reference folder of any of your libraries.
- Fixed: organising an existing folder into a library could register it as a
  reference folder inside another library's folder.
