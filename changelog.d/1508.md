- A workflow whose checkpoint won't load now says "Checkpoint missing" in the
  Workflows panel and names the file it is missing (hover for the full path),
  instead of showing the name as if nothing were wrong or "Not recorded". The
  grid card says "Checkpoint missing" too.
- A workflow whose checkpoint name was never recorded or was forgotten now
  shows the file its graph loads, and one that loads no base model at all
  says so.
