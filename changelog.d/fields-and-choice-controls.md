- Text fields are one height everywhere and match the buttons beside them.
  Labels sit above a field rather than inside it, and folder paths and ports
  are shown in a monospaced face.
- Choosing one of a few options is a single segmented control across the app,
  and sort menus mark the current choice with a check instead of a filled row.
  Both can be moved through with the arrow keys.
