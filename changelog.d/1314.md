- Deleting a model from the Models shelf now says what it leaves behind: which
  VAEs and text encoders only ran with the models you are deleting, which ones
  other models still use, and when PixlStash cannot tell. It reads this from
  the workflows behind your pictures.
