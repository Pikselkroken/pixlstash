- Deleting a model from the Models shelf now says what it leaves behind: which
  VAEs and text encoders nothing else would use, which ones other models still
  need, and when PixlStash cannot tell. It reads this from the workflows behind
  your pictures.
