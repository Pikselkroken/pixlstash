- Drop a ComfyUI workflow file onto the Workflows view to add it. It is kept
  exactly as it is, in either of ComfyUI's formats, and dropping a copy of a
  workflow you already have takes you to that one instead of adding it twice.
- Importing a workflow no longer asks you to map its image and caption inputs:
  PixlStash finds them in the graph. Workflows you imported before keep working
  as they did.
