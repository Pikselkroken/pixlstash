- Fixed: the full-screen viewer now follows an in-app ComfyUI image-to-image or
  upscale run to the picture it produced, instead of staying on the one you
  started from.
- Fixed: the address bar keeps up when the viewer moves to a picture a ComfyUI
  or plugin run just created, so reloading the page or sharing the link no
  longer takes you back to the previous picture.
