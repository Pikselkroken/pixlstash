- A workflow saved from ComfyUI in its editor format can now be made runnable:
  converting it from ComfyUI (with a ComfyUI-PixlStash node release that has
  *Convert for PixlStash*) gives its card parameters and lets Run use it. Until
  then the card says its parameters are not available yet.
