- The filter menu's Problems section now has "No character" and "In no set" as
  two separate choices. "No character" used to also hide every picture that
  was in a set; now it shows exactly the pictures with nobody named in them.
  Tick both to get the old combined view back.
