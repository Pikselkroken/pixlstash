- Fixed: replacing a picture set's members through the API with an empty list
  no longer silently empties the set. It is refused unless you pass
  `allow_empty: true`, and the reply now says how many pictures were removed.
- Fixed: a non-numeric picture id on the set-member API routes is now a clear
  422 instead of a 500 "Internal Server Error".
