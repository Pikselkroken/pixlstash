- Animated GIFs are now looked at across several frames, like videos: faces
  that appear after the first frame are found, the caption can come from a
  later frame, and similarity and near-duplicate matching use the whole
  animation rather than its first frame. GIFs already in your library are
  re-processed once after the update.
