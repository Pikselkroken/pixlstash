- Animated GIFs are now looked at across several frames, like videos: faces
  that appear after the first frame are found, a caption can come from a later
  frame when the first gives none, and similarity and near-duplicate matching
  use an embedding averaged over the animation rather than its first frame.
  GIFs already in your library get the new similarity once after the update;
  their faces are kept as they are.
