- Fixed: a workflow saved in ComfyUI with a bypassed node wired between two
  others is now recognised as the same workflow your pictures were made with,
  instead of showing up as a separate one. Running such a workflow from
  PixlStash also passes the wire through the bypassed node the way ComfyUI
  does.
