- Changed: on the Workflows screen, an open stack now follows what you
  select. Clicking another stack moves the panel to that one, and selecting
  anything outside the open stack closes it. The panel grows and shrinks
  rather than appearing and disappearing, so the rows below it can be seen
  moving. Opening a stack is unchanged: the caret, Enter, or a double-click.
