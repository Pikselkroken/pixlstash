- Changed: on the Workflows screen, an open stack now follows what you
  select. Clicking another stack moves the panel to that one, and selecting
  a workflow outside the stack puts the panel away — pick a stack again and
  it comes straight back, with no second double-click. The panel slides open
  and shut instead of blinking in and out. Opening the first one is
  unchanged: the caret, Enter, or a double-click.
