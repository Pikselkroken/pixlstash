- Changed: on the Workflows screen, an open stack now follows what you
  select. Clicking another stack moves the panel to that one, and selecting
  anything outside the open stack takes it off the screen — pick a stack
  again and it comes back, without a second double-click. The panel grows
  and shrinks rather than appearing and disappearing, so the rows below it
  can be seen moving. Opening the first stack is unchanged: the caret,
  Enter, or a double-click.
