- The stats panel and the workflow inspector are one panel: tabs across the
  top, and group names in the same small capitals the lightbox uses.
- The selection pill looks the same on the photo grid, the model shelf and
  training runs.
