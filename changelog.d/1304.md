- A `workflows` folder in PixlStash's data folder is watched: put a ComfyUI
  workflow file in it and it is added, the same as dropping it on the
  Workflows view. Removing a file from the folder never deletes a workflow.
- Deleting a workflow moves a copy of it to your system trash. Restore it from
  the trash and it is added again.
