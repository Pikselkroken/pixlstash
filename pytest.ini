[pytest]
# Only collect the project's own tests. Without this, bare `pytest` recurses
# into build artifacts such as electron/release/.../site-packages (a bundled
# Python env whose third-party test modules fail to import).
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
